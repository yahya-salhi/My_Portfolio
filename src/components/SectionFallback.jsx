import { styles } from "../styles";

const SectionFallback = () => (
  <div
    className={`${styles.padding} max-w-7xl mx-auto min-h-[120px]`}
    aria-hidden
  />
);

export default SectionFallback;
